{
  "name": "hangar",
  "private": true,
  "version": "0.1.0",
  "description": "Ship with confidence. Desktop control center for Expo / React Native apps.",
  "scripts": {
    "dev": "pnpm --filter @hangar/desktop dev",
    "build": "pnpm -r build",
    "build:core": "pnpm --filter @hangar/core build",
    "build:desktop": "pnpm --filter @hangar/desktop build",
    "cli": "pnpm --filter @hangar/cli start",
    "typecheck": "pnpm -r typecheck",
    "test": "pnpm -r --if-present test",
    "lint": "pnpm -r lint"
  },
  "engines": {
    "node": ">=20",
    "pnpm": ">=9"
  },
  "packageManager": "pnpm@10.33.0"
}
