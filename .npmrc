# Pin to exact versions on install/add — no implicit ^ or ~
save-exact=true
save-prefix=

# Fail fast if peer dependencies are missing
strict-peer-dependencies=true

# Disallow arbitrary postinstall scripts unless explicitly approved
# (allow list is configured in pnpm-workspace.yaml under onlyBuiltDependencies)
side-effects-cache=false
